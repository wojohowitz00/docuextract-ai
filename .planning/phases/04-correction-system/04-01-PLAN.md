---
phase: 04-correction-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/database.py
  - backend/main.py
  - tests/test_database.py
  - tests/test_api.py
autonomous: true
must_haves:
  truths:
    - "Database has a `corrections` table to track history"
    - "User can update extraction fields via API"
    - "Original values are preserved in the audit trail"
  artifacts:
    - backend/database.py
    - backend/main.py
  key_links:
    - "PUT /api/extractions/{id} updates the `extractions` table and logs to `corrections`"
---

<objective>
Implement backend support for corrections and audit trail.

Purpose: Enable users to correct extraction errors and maintain a history of changes (Req CORR-01, CORR-03).

Output: Updated database schema and API endpoints for extraction updates.
</objective>

<execution_context>
@/Users/richardyu/.gemini/get-shit-done/workflows/execute-plan.toml
@/Users/richardyu/.gemini/get-shit-done/templates/summary.toml
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@backend/database.py
@backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update database schema and functions</name>
  <files>backend/database.py, tests/test_database.py</files>
  <action>
  1. Modify `init_database` in `backend/database.py`:
     - Create `corrections` table: `id`, `extraction_id`, `field_name`, `old_value`, `new_value`, `created_at`.
  2. Implement `update_extraction` function:
     - Accepts `extraction_id` and dictionary of fields to update.
     - Fetches current values first.
     - For each changed field, inserts record into `corrections` table.
     - Updates `extractions` table with new values.
     - Handles `line_items` updates (replace all or smart diff? For v1, replace all line items if provided).
  3. Add tests in `tests/test_database.py` to verify:
     - `update_extraction` correctly updates fields.
     - `corrections` table records the changes.
  </action>
  <verify>
  Run: `uv run pytest tests/test_database.py`
  Expected: All tests pass, including new update/audit tests.
  </verify>
  <done>
  Database supports updates and correction logging. Tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add update endpoint to API</name>
  <files>backend/main.py, tests/test_api.py</files>
  <action>
  1. Add `PUT /api/extractions/{extraction_id}` endpoint in `backend/main.py`.
     - Accepts JSON body with fields to update.
     - Calls `database.update_extraction`.
     - Returns updated extraction data.
  2. Add tests in `tests/test_api.py`:
     - Test `PUT` endpoint with valid data.
     - Test with non-existent ID (404).
  </action>
  <verify>
  Run: `uv run pytest tests/test_api.py`
  Expected: New endpoint works and passes tests.
  </verify>
  <done>
  API supports updating extractions.
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest` passes.
2. `PUT /api/extractions/{id}` updates data and creates audit log.
</verification>

<success_criteria>
- Backend supports full update lifecycle
- Audit trail works (CORR-03)
</success_criteria>

<output>
After completion, create `.planning/phases/04-correction-system/04-01-SUMMARY.md`
</output>
