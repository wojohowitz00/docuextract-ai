---
phase: 04-correction-system
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - components/ResultsTable.tsx
  - services/backendService.ts
autonomous: true
user_setup: []
must_haves:
  truths:
    - "User can toggle 'Edit' mode on any extraction result"
    - "User can modify key fields (Vendor, Date, Amounts) in the UI"
    - "Saving sends a PUT request to the backend"
    - "UI updates immediately with new values/amounts"
  artifacts:
    - components/ResultsTable.tsx
  key_links:
    - "ResultsTable calls backendService.updateExtraction"
---

<objective>
Implement inline editing in the React frontend.

Purpose: Allow users to correct extraction errors directly in the UI (Req CORR-01).

Output: Updated `ResultsTable` with edit/save functionality.
</objective>

<execution_context>
@/Users/richardyu/.gemini/get-shit-done/workflows/execute-plan.toml
@/Users/richardyu/.gemini/get-shit-done/templates/summary.toml
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@components/ResultsTable.tsx
@services/backendService.ts
@types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add update service method</name>
  <files>services/backendService.ts</files>
  <action>
  1. Add `updateExtraction` to `services/backendService.ts`:
     - Signature: `(id: string, updates: Partial<ExtractedData>) => Promise<ExtractedData>`
     - Calls `PUT /api/extractions/{id}` (implemented in Plan 04-01).
  </action>
  <verify>
  Check file content.
  </verify>
  <done>
  Service method exists and calls correct endpoint.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor ResultsTable and Add Edit Mode</name>
  <files>components/ResultsTable.tsx</files>
  <action>
  1. Refactor `ResultsTable.tsx`:
     - Extract `FinancialDocumentView` (for Invoice, Receipt, etc.)
     - Extract `ResearchPaperView`
     - Extract `FinancialReportView`
     - Keep `ResultsTable` as the switcher component.
  
  2. Implement inline editing in each View component:
     - Add `isEditing` state.
     - Add local state for form data (initialized from props).
     - Render `<input>` fields when `isEditing` is true, otherwise render display spans.
     - Add "Edit", "Save", "Cancel" buttons next to "Download CSV".
     - Implement `handleSave`: 
       - Call `backendService.updateExtraction`.
       - On success, exit edit mode and call `onUpdate` prop (needed to refresh parent state).
  
  3. Update `App.tsx` (or whatever parent uses `ResultsTable`) to pass an `onUpdate` callback if needed, or simply rely on local state update if `ResultsTable` manages the displayed data. Since `ResultsTable` takes `data` prop, the parent fetching logic might need a refresh trigger. For v1, simpler to just update the local data copy in `ResultsTable` or pass an `onUpdate` callback up.
     - *Decision*: Add `onUpdate` callback prop to `ResultsTable` and its children.
  </action>
  <verify>
  Since we can't easily run browser tests autonomously yet without setup:
  1. Verify code structure (subcomponents extracted).
  2. Verify logic (isEditing toggles inputs).
  3. Verify API call connection (updateExtraction called on save).
  </verify>
  <done>
  ResultsTable supports inline editing for all document types and calls the backend API.
  </done>
</task>

</tasks>

<verification>
1. Code review shows `updateExtraction` call.
2. Edit state toggles inputs.
3. Subcomponents exist.
</verification>

<success_criteria>
- User can correct wrong extractions
- UI handles the update flow
</success_criteria>

<output>
After completion, create `.planning/phases/04-correction-system/04-02-SUMMARY.md`
</output>
