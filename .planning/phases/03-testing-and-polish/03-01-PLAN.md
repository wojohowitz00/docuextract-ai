---
phase: 03-testing-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/conftest.py
  - tests/test_extraction.py
  - tests/test_models.py
  - tests/test_pdf_parser.py
autonomous: true
must_haves:
  truths:
    - "All existing unit tests pass without errors or warnings"
    - "Extraction pipeline tests validate the three-stage flow (OCR → classify → extract)"
    - "PDF parser tests cover text, vision, and hybrid strategies"
    - "Model tests validate both old EOB schemas and new ExtractedData model"
  artifacts:
    - tests/conftest.py
    - tests/test_extraction.py
    - tests/test_models.py
    - tests/test_pdf_parser.py
  key_links:
    - "conftest fixtures used by all test files"
    - "test_models.py validates backend/models.py (ExtractedData, DocumentType, LineItem)"
---

<objective>
Fix and validate the existing test suite so all tests pass cleanly.

Purpose: The test suite has 5 test files but `uv run pytest --co` only showed a deprecation warning and no collected tests. This plan diagnoses and fixes test collection/execution issues, ensuring the test foundation is solid before doing E2E testing.

Output: A passing test suite with `uv run pytest -v` showing green across all modules.
</objective>

<execution_context>
@/Users/richardyu/.gemini/get-shit-done/workflows/execute-plan.toml
@/Users/richardyu/.gemini/get-shit-done/templates/summary.toml
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key files:
@tests/conftest.py
@tests/test_api.py
@tests/test_database.py
@tests/test_extraction.py
@tests/test_models.py
@tests/test_pdf_parser.py
@backend/models.py
@backend/extraction.py
@backend/pdf_parser.py
@models/schemas.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Diagnose and fix test collection</name>
  <files>pyproject.toml, tests/conftest.py</files>
  <action>
  1. Fix the `pyproject.toml` deprecation warning: change `[tool.uv.dev-dependencies]` to `[dependency-groups]` with a `dev` group.
  2. Add pytest configuration in `pyproject.toml` under `[tool.pytest.ini_options]`:
     - `testpaths = ["tests"]`
     - `pythonpath = ["."]`
  3. Run `uv run pytest --co -v` to verify all tests are discovered and collectible.
  4. If any import errors occur, fix them (likely path issues with `backend.` prefix).
  </action>
  <verify>
  Run: `uv run pytest --co -v 2>&1`
  Expected: All test functions listed, no ImportError or collection errors.
  </verify>
  <done>
  `uv run pytest --co` shows all test functions from all 5 test files collected without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite, fix failures</name>
  <files>tests/test_extraction.py, tests/test_models.py, tests/test_pdf_parser.py</files>
  <action>
  1. Run `uv run pytest -v --tb=short` to execute all tests.
  2. For each failing test:
     - If it's an import issue (e.g. `models.schemas` vs `backend.models`), fix the import.
     - If it's a mock issue (stale function signatures), update mock to match current code.
     - If it's a test that references old EOB-specific logic that no longer exists, update or remove the test.
  3. Ensure `test_models.py` tests the current `backend/models.py` models (ExtractedData, DocumentType, LineItem) not just the old `models/schemas.py` models.
  4. Ensure `test_extraction.py` validates the three-stage pipeline with appropriate mocks for Ollama calls.
  5. Ensure `test_pdf_parser.py` covers text, vision, and hybrid parsing strategies.
  </action>
  <verify>
  Run: `uv run pytest -v --tb=short 2>&1`
  Expected: All tests pass (green), no warnings except acceptable ones.
  </verify>
  <done>
  `uv run pytest` exits with code 0. All tests pass. No test references stale models or dead code.
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest -v` shows all tests collected and passing
2. No deprecation warnings from pyproject.toml
3. Tests cover: API endpoints, database CRUD, extraction pipeline, data models, PDF parsing
</verification>

<success_criteria>
- `uv run pytest` exits 0 with all tests green
- Test suite covers all 5 backend modules
- No stale test code referencing old EOB-only models exclusively
</success_criteria>

<output>
After completion, create `.planning/phases/03-testing-and-polish/03-01-SUMMARY.md`
</output>
