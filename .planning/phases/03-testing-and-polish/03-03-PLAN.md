---
phase: 03-testing-and-polish
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - models/schemas.py
  - models/__init__.py
  - backend/models.py
autonomous: true
must_haves:
  truths:
    - "No dead code directories remain (extractor/, gui/, database/)"
    - "models/schemas.py is either removed or consolidated into backend/models.py"
    - "All imports across the project resolve correctly after cleanup"
    - "Tests still pass after code cleanup"
  artifacts:
    - backend/models.py
  key_links:
    - "backend/models.py is the canonical data model for the extraction pipeline"
    - "No code imports from models/schemas.py"
---

<objective>
Remove dead code and consolidate data models into a single source of truth.

Purpose: The codebase has stale models (`models/schemas.py` with 11 old EOB classes never imported by the current backend), empty directories (`extractor/`, `gui/`, `database/`), and duplicate model definitions. Cleaning this up prevents confusion in future sessions.

Output: A clean project tree with one canonical model file and no dead directories.
</objective>

<execution_context>
@/Users/richardyu/.gemini/get-shit-done/workflows/execute-plan.toml
@/Users/richardyu/.gemini/get-shit-done/templates/summary.toml
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Key files:
@models/schemas.py (OLD: 11 EOB-specific Pydantic classes, not used by backend)
@backend/models.py (CURRENT: ExtractedData, DocumentType, LineItem — used by extraction pipeline)
@backend/extraction.py (imports from backend.models)
@backend/main.py (imports from backend.models)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead directories and stale model file</name>
  <files>models/schemas.py, models/__init__.py, extractor/, gui/, database/</files>
  <action>
  1. Verify no code imports from `models/schemas.py`:
     Run: `grep -r "from models" . --include="*.py" | grep -v __pycache__ | grep -v test`
     Run: `grep -r "import schemas" . --include="*.py" | grep -v __pycache__`
  
  2. If no imports found:
     - Delete `models/schemas.py` and `models/__init__.py`
     - Remove the `models/` directory entirely
  
  3. Remove empty directories (only contain `__pycache__/`):
     - `rm -rf extractor/`
     - `rm -rf gui/`
     - `rm -rf database/` (check it has no code first — if it has actual Python files, keep them)
  
  4. Update `.gitignore` if needed — no changes expected since `__pycache__/` is already gitignored.
  
  5. Verify `backend/models.py` is the sole model file and all imports work:
     Run: `uv run python -c "from backend.models import ExtractedData, DocumentType, LineItem; print('OK')"`
  </action>
  <verify>
  Run: `find . -type d -empty | grep -v node_modules | grep -v .git | grep -v .beads`
  Expected: No empty directories in the project (other than node_modules internals).
  Run: `ls models/ 2>&1` should fail (directory removed).
  Run: `ls extractor/ 2>&1` should fail.
  Run: `ls gui/ 2>&1` should fail.
  </verify>
  <done>
  Dead directories removed. `models/schemas.py` deleted. `backend/models.py` is the single source of truth for data models.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify tests pass after cleanup</name>
  <files>tests/test_models.py</files>
  <action>
  1. Run `uv run pytest -v --tb=short` to verify all tests still pass after code removal.
  2. If `test_models.py` imported from `models.schemas`, update it to import from `backend.models`.
  3. Fix any other broken imports.
  </action>
  <verify>
  Run: `uv run pytest -v --tb=short 2>&1`
  Expected: All tests pass. No import errors.
  </verify>
  <done>
  Full test suite passes. No stale imports remain.
  </done>
</task>

</tasks>

<verification>
1. `uv run pytest -v` passes all tests
2. `find . -name "schemas.py" -not -path "*/node_modules/*"` returns no results
3. `ls models/ extractor/ gui/ database/ 2>&1` all show "No such file or directory"
4. `grep -r "from models" . --include="*.py" | grep -v __pycache__ | grep -v node_modules` returns no results
5. Project tree is clean: only `backend/`, `components/`, `data/`, `docs/`, `services/`, `tests/`, `.planning/`, `.beads/`
</verification>

<success_criteria>
- No dead directories or unused model files remain
- `backend/models.py` is the canonical model source
- Test suite passes after cleanup
- `git status` shows only file deletions and modifications, all intentional
</success_criteria>

<output>
After completion, create `.planning/phases/03-testing-and-polish/03-03-SUMMARY.md`
</output>
